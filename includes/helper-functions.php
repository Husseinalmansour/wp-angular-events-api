<?php
if ( ! defined( 'ABSPATH' ) ) {
    exit; // Exit if accessed directly.
}

// Example helper function (not used yet)
function customapi_sanitize_input( $input ) {
    return sanitize_text_field( $input );
}